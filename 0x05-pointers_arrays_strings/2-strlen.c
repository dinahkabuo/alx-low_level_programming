#include <string.h>
#include "main.h"
/**
 * _strlen - returns length of string
 * @s: string to be evaluated
 * Return: length of string
 */
int _strlen(char *s)
{
	int 1;

	1 = strlen(s);
	return (1);
}
